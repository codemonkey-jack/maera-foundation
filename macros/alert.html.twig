{% macro alert(type, message, id, class, close) %}
<div class="alert-box{{ type ? ' '~type : ' secondary' }} {{ class|default('') }}">
{{ message|raw }}
{% if close|default(false) %}<a href="#" class="close">&times;</a>{% endif %}
</div>
{% endmacro %}
